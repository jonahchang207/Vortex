#include "vortex/control/profile.hpp"

namespace vortex {
    // Basic implementation is currently header-only in this simple case,
    // but we can add more complex profiling logic here if needed.
}
